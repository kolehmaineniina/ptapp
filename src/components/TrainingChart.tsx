import { BarChart } from "@mui/icons-material";
import { Training } from "../types";

export default function TrainingChart({trainings}: {trainings: Training[]}) {

    

    return (
        <BarChart>
            
        </BarChart>
    );

}